using AddApplication.Models;
using AddApplication.Utils;
using RestSharp;
using System;
using System.Collections.Generic;
using System.Text;

namespace AddApplication.Src.Http
{
    class Request
    {






        //public static ApiResponseModel Package(string package)
        //{
        //    ApiRequestModel request = ApiRequestModel(package);

        //    var client = new RestClient("https://" + request.Uri + request.UriPath);
        //    var restRequest = new RestRequest();
        //    foreach (KeyValuePair<string, string> entry in request.Headers)
        //    {
        //        restRequest.AddHeader(entry.Key, entry.Value);
        //    }

        //    return Create.ResponsePackage(
        //        client.Get(restRequest)
        //    );
        //}
    }
}
